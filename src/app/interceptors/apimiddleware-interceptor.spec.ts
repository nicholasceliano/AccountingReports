import { APIMiddlewareInterceptor } from './apimiddleware-interceptor';

describe('APIMiddlewareInterceptor', () => {
	it('should create an instance', () => {
		expect(new APIMiddlewareInterceptor()).toBeTruthy();
	});
});
